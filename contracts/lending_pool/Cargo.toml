[package]
name = "lending_pool"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "20.5"
derive_arbitrary = "=1.3.2"

[dev-dependencies]
soroban-sdk = { version = "20.5", features = ["testutils"] }
